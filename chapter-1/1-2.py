import math

if __name__=='__main__':
    x = math.sqrt(5)
    y = math.ceil(x)
    z = math.floor(x)
    xx = round(x)
    yy = x ** 2
    print(x, y, z, xx, yy)

    arr = [1, 4, -1, 9]
    min_arr = min(arr)
    max_arr = max(arr)

    print(min_arr, max_arr)


